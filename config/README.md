# Config (`config/`)

Default user-facing configuration templates.

## Files

- `default.toml`: baseline provider/storage defaults used when user config is missing.

## Notes

- Keep defaults safe and local-first.
- Runtime/env overrides are handled in core config module.
