# GUI Rust (`gui/rust/`)

Rust code used by GUI-side integration.

## Folders

- `chat_controller/`: static Rust backend linked by CMake into Qt executable.

This folder is GUI-facing Rust, separate from the reusable core crate.
